# Docker Compose for development/testing convenience
# Not required to use the scanner - just run: docker run vulnscanner:local --url <target>

version: '3.8'

services:
  scanner:
    build: .
    image: vulnscanner:local

networks:
  default:
    name: scanner-test-net
